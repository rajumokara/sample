const width = window.innerWidth;
const height = window.innerHeight;

export const mainWidth = width - 130;
export const bannerWidth = width - 100;
export const loginHeight = height;
export const loginInnerHeight = height - 100;

export const receiptInnerHeight = height - 200;
